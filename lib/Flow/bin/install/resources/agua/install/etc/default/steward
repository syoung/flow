# Steward Upstart and SysVinit configuration file

# Customize location of Steward binary (especially for development testing).
#STEWARD="/usr/local/bin/steward"

# Use STEWARD_OPTS to modify the daemon startup options.
#STEWARD_OPTS="--showlog 4 --printlog 5"

# If you need Steward to use a specific environment variable, it can also be specified here.

export PERL5LIB=/a/extlib/linux/lib/perl5:/a/extlib/linux/lib/perl5/x86_64-linux-gnu-thread-multi:/a/lib

export SOME_ENVIRONMENT_VARIABLE=somevalue

